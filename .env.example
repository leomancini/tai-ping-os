# Anthropic API key used server-side by server.js to generate apps.
# Create a key at https://console.anthropic.com/ and put it in a local `.env`
# (which is git-ignored). NEVER commit this value or expose it to the browser.
ANTHROPIC_API_KEY=
